<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>
    </head>

    <body>
        <h1>{{title}}</h1>

        {{#each students}}
            <p>
                <a href="/students/{{id}}"> {{id}} </a> {{name}}
            </p>

        {{/each}}
       

    </body>
</html>